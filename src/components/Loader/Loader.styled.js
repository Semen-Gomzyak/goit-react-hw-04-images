import styled from 'styled-components';
import { Audio } from 'react-loader-spinner';

export const AnimatedLoader = styled(Audio)`
    height = "80"
    width = "80"
    radius = "9"
    color = 'green'
    ariaLabel = 'three-dots-loading'     
    wrapperStyle
    wrapperClass
`;
